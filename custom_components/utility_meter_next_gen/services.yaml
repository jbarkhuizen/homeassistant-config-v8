# Describes the format for available switch services

reset:
  target:
    entity:
      domain: select
      integration: utility_meter_next_gen

calibrate:
  target:
    entity:
      domain: sensor
      integration: utility_meter_next_gen
  fields:
    value:
      example: "100"
      required: true
      selector:
        text:
